- name: variables from files
  hosts: local
  connection: local
  vars_files:
  - course.yaml
  tasks:
  - name: print course info
    ansible.builtin.debug:
      msg: "course name is: {{ COURSE }}, Duration is: {{ DURATION }}, trainer is {{ Trainser }}"